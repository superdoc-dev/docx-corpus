services:
  scraper:
    build: .
    container_name: docx-corpus-scraper
    volumes:
      - ./corpus:/app/corpus
    env_file:
      - .env
    deploy:
      resources:
        limits:
          memory: ${MEMORY_LIMIT:-8G}
